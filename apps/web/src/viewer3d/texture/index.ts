// viewer3d/texture — 전개도→텍스처 굽기 엔트리(ViewModel).
export {
  buildNetSvg,
  netInnerMarkup,
  rasterizeNetSvg,
  netTextureSize,
  NET_PX_PER_CM,
  NET_MAX_PX,
} from './bakeNet';
export { buildTopThumbnail, buildTopThumbnailSvg, THUMBNAIL_MAX_PX } from './topThumbnail';
