# api — 백엔드 호출 클라이언트

> 제품 요구사항(기능 ID 정의)은 루트 [CLAUDE.md](../../../../CLAUDE.md)의 **PRD** 섹션 참조.

프론트엔드에서 `apps/api`를 호출하는 클라이언트 계층.

## 담는 것
- 디자인 저장·조회, 링크 발급/검증, 이미지 업로드 호출
- 요청/응답 타입은 `packages/shared/schema` 재사용

## 규칙
- UI·상태와 분리된 얇은 호출 계층으로 유지.
- 비즈니스 로직을 여기 두지 말 것 (호출·직렬화만).
